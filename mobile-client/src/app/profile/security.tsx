import React,{useState} from 'react';
import {Alert,KeyboardAvoidingView,Platform,StyleSheet,Text,TouchableOpacity,View} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context'; import {useRouter} from 'expo-router'; import {Ionicons} from '@expo/vector-icons';
import {Button,Input} from '@/components/ui'; import {apiClient} from '@/services/api'; import {COLORS,SPACING,FONTS} from '@/constants/theme';
export default function SecurityScreen(){const router=useRouter();const[current,setCurrent]=useState(''),[next,setNext]=useState(''),[confirm,setConfirm]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');const save=async()=>{if(next.length<8){setError('La nueva contraseña debe tener al menos 8 caracteres');return;}if(next!==confirm){setError('Las contraseñas no coinciden');return;}setError('');setLoading(true);try{await apiClient.changePassword(current,next);Alert.alert('Contraseña actualizada','Tu contraseña se cambió correctamente',[{text:'Aceptar',onPress:()=>router.back()}]);}catch(e:any){Alert.alert('Error',e?.response?.data?.error||'No se pudo cambiar la contraseña');}finally{setLoading(false);}};return <SafeAreaView style={s.container}><View style={s.header}><TouchableOpacity onPress={()=>router.back()}><Ionicons name="arrow-back" size={26} color={COLORS.textPrimary}/></TouchableOpacity><Text style={s.title}>Seguridad</Text><View style={{width:26}}/></View><KeyboardAvoidingView style={s.content} behavior={Platform.OS==='ios'?'padding':undefined}><Text style={s.heading}>Cambiar contraseña</Text><Text style={s.help}>Utiliza al menos 8 caracteres.</Text><Input label="Contraseña actual" value={current} onChangeText={setCurrent} secureTextEntry/><Input label="Nueva contraseña" value={next} onChangeText={setNext} secureTextEntry error={error}/><Input label="Confirmar nueva contraseña" value={confirm} onChangeText={setConfirm} secureTextEntry/><Button title="Actualizar contraseña" onPress={save} disabled={!current||!next||!confirm} loading={loading} fullWidth/></KeyboardAvoidingView></SafeAreaView>}
const s=StyleSheet.create({container:{flex:1,backgroundColor:COLORS.background},header:{height:58,flexDirection:'row',alignItems:'center',justifyContent:'space-between',paddingHorizontal:SPACING.lg,backgroundColor:COLORS.surface,borderBottomWidth:1,borderBottomColor:COLORS.border},title:{fontSize:FONTS.sizes.lg,fontWeight:'700',color:COLORS.textPrimary},content:{flex:1,padding:SPACING.lg},heading:{fontSize:FONTS.sizes.xl,fontWeight:'700',color:COLORS.textPrimary,marginTop:SPACING.lg},help:{color:COLORS.textSecondary,marginTop:SPACING.xs,marginBottom:SPACING.xl}});
